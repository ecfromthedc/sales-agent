<!-- Keep each PR scoped to a SINGLE Linear ticket. Split unrelated work into separate PRs. -->

**Linear:** SALE-___ <!-- Required. Replace ___ with the ticket number so Linear auto-links this PR. -->

## Summary

<!-- What changed and why. One or two tight paragraphs. -->

## Testing

- [ ] `npm run typecheck` passes (`tsc --noEmit`)
- [ ] `npm test` passes (`vitest run`)
- [ ] Manually verified the change where applicable (e.g. `npm run dev`)

## Scope

- [ ] This PR implements exactly **one** ticket — no unrelated changes.
